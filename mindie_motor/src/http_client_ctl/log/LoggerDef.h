/*
 * Copyright (c) Huawei Technologies Co., Ltd. 2025-2025. All rights reserved.
 * MindIE is licensed under Mulan PSL v2.
 * You can use this software according to the terms and conditions of the Mulan PSL v2.
 * You may obtain a copy of Mulan PSL v2 at:
 *         http://license.coscl.org.cn/MulanPSL2
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND,
 * EITHER EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT,
 * MERCHANTABILITY OR FIT FOR A PARTICULAR PURPOSE.
 * See the Mulan PSL v2 for more details.
 */
#ifndef MINDIE_MS_LOG_DEF_H
#define MINDIE_MS_LOG_DEF_H

#include <cstdint>
namespace MINDIE {
namespace MS {
constexpr uint32_t DEFAULT_MAX_LOG_FILE_SIZE = 20; // 20M
constexpr uint32_t DEFAULT_MAX_LOG_FILE_NUM = 10;
constexpr uint32_t DEFAULT_LOG_ROTATE_DAY = 30;


enum class LogLevel {
    MINDIE_LOG_CRITICAL = 0,
    MINDIE_LOG_ERROR,
    MINDIE_LOG_WARN,
    MINDIE_LOG_INFO,
    MINDIE_LOG_DEBUG,
    MINDIE_LOG_PERF,
    MINDIE_LOG_UNKNOWN,
    MINDIE_LOG_DISABLE
};

enum class LogType {
    RUN = 0,
    OPERATION = 1
};

enum class SubModule {
    MS_CONTROLLER = 1,
    MS_COORDINATOR,
    MS_DEPLOYER,
    MS_COMMON,
    MS_NONAME
};

enum class ErrorType {
    EXCEPTION = 0,
    INVALID_INPUT,
    INVALID_PARAMETER,
    OUT_OF_RANGE,
    OPERATION_REPEAT,
    NOT_FOUND,
    RESOURCE_LIMIT,
    RESOURCE_EXHAUSTED,
    DEADLINE_EXCEEDED,
    UNREACHABLE,
    CALL_ERROR,
    UNAVAILABLE,
    FAILED_PRECONDITION,
    PENDING,
    DATA_LOSS,
    UNAUTHENTICATED,
    PERMISSION_DENIED,
    WARNING,
    INTERNAL_UNKNOWN = 255,     // 保留用于内部未知错误
    INVALID_ENUM
};

enum class ControllerFeature {
    CONTROLLER = 0,
    MAIN,
    CLUSTER_STATUS_WRITER,
    COORDINATOR_STORE,
    NODE_STATUS,
    ALARM_LISTENER,
    ALARM_MANAGER,
    CONSTANT,
    JSON_FILE_LOADER,
    NODE_SCHEDULER,
    GROUP_GENERATOR,
    RANK_TABLE_LOADER,
    ROLE_MANAGER_INITIALIZER,
    ROLE_SWITCHER,
    PROBE_SERVER,
    PROCESS_MANAGER,
    COORDINATOR_REQUEST_HANDLER,
    SERVER_REQUEST_HANDLER,
    STATUS_UPDATER,
    FAULT_MANAGER,
    LEADER_AGENT,
    RESOURCE_MANAGER,
    INVALID_ENUM
};

enum class CoordinatorFeature {
    COORDINATOR = 0,
    MAIN,
    CLUSTER_NODES,
    CONNECTION_POOL,
    CLIENT_CONNECTION,
    HTTPCLIENT_ASYNC,
    HTTPSERVER,
    SERVER_CONNECTION,
    SSLSERVER_CONNECTION,
    HTTPIO_CTXPOOL,
    CONFIGURE,
    CONTROLLER_LISTENER,
    REQUEST_LISTENER,
    REQUEST_REPEATER,
    D_EXCEPTIONHANDLER,
    P_EXCEPTIONHANDLER,
    REQTIMEOUT_EXCEPTIONHANDLER,
    SINGLE_EXCEPTIONHANDLER,
    TOKENIZER_EXCEPTIONHANDLER,
    D_REQUESTREPEATER,
    P_REQUESTREPEATER,
    TOKENIZER_REPEATER,
    SINGLEREQUEST_REPEATER,
    DEFAULT_SCHEDULER,
    BASEALGORITHM,
    PREFIXCACHE_EXECUTOR,
    ROUNDROBIN_EXECUTOR,
    LOADBALANCE_EXECUTOR,
    METRICS,
    REQMANAGE,
    USERDISCON_EXCEPTIONHANDLER,
    INVALID_ENUM
};

enum class DeployerFeature {
    CROSSNODE_SERVER = 0,
    KUBERESOURCE_DEF,
    SERVER_FACTORY,
    SERVER_MANAGER,
    SINGLENODE_SERVER,
    STATUS_HANDLER,
    MSCTL,
    SERVER,
    INVALID_ENUM
};

enum class CommonFeature {
    HTTPSERVER = 0,
    DIGS,
    HTTPCLIENT,
    UTIL,
    LOGGER,
    INVALID_ENUM,
    ALARM_REQUEST_HANDLER
};
}
}
#endif